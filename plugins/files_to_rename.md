# Batch File Renamer (`files_to_rename.py`)

In-place rename — no output directory. Standalone GUI or host tool.

```bash
python plugins/files_to_rename.py
python plugins/files_to_rename.py -i "*.txt" --prefix draft_
```

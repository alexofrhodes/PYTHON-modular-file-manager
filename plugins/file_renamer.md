# Batch File Renamer (`file_renamer.py`)

In-place rename — no output directory. Standalone GUI or host tool.

```bash
python plugins/file_renamer.py
python plugins/file_renamer.py -i "*.txt" --prefix draft_
```
